services:
  multas-front-qa:
    container_name: multas-front-qa
    build:
      context: ../../
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "4201:80"
    networks:
      - multas_network
    labels:
      - com.multas.environment=qa
    environment:
      - API_URL=${API_URL}

networks:
  multas_network:
    external: true
